version: '3'
services:
    player:
        build: player/.
    user:
        build: user/.
    playlist:
        build: playlist/.
    nginx:
        image: nginx
        ports:
            - "8080:8080"
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf:ro
            - ./client/build:/etc/nginx/html:ro
